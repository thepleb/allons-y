<%= form_for @event, html: { class: "pure-form" } do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :starts %><br>
    <%= f.datetime_select :starts %>
  </div>
  <div class="actions">
    <%= f.submit "Create Event", class: "pure-button pure-button-primary" %>
  </div>
<% end %>
